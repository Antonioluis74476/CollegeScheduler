@page "/admin/event-statuses/create"
@attribute [Authorize(Roles = "Admin")]
@rendermode InteractiveServer

@using Microsoft.AspNetCore.Authorization
@using CollegeScheduler.DTOs.Scheduling
@using CollegeScheduler.Services.Admin

@inject IEventStatusService EventStatusService
@inject NavigationManager Navigation

<PageTitle>Create Event Status</PageTitle>

<h3>Create Event Status</h3>

@if (!string.IsNullOrWhiteSpace(_errorMessage))
{
    <div class="alert alert-danger">
        @_errorMessage
    </div>
}

<EditForm Model="_model"
          OnValidSubmit="CreateStatus">

    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label class="form-label">Name</label>

        <InputText class="form-control"
                   @bind-Value="_model.Name" />
    </div>

    <button type="submit"
            class="btn btn-primary me-2"
            disabled="@_isSaving">
        @(_isSaving ? "Saving..." : "Create")
    </button>

    <button type="button"
            class="btn btn-secondary"
            @onclick="GoBack">
        Cancel
    </button>
</EditForm>

@code
{
    private EventStatusCreateDto _model = new();
    private string? _errorMessage;
    private bool _isSaving;

    private async Task CreateStatus()
    {
        _errorMessage = null;

        if (string.IsNullOrWhiteSpace(_model.Name))
        {
            _errorMessage = "Name is required.";
            return;
        }

        _isSaving = true;

        var success = await EventStatusService.CreateAsync(_model.Name);

        _isSaving = false;

        if (success)
        {
            Navigation.NavigateTo("/admin/event-statuses");
            return;
        }

        _errorMessage = "The event status could not be created.";
    }

    private void GoBack()
    {
        Navigation.NavigateTo("/admin/event-statuses");
    }
}